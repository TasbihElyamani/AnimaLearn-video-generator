Transfer learning reuses a pre-trained model on a new task. Fine-tuning: unfreeze some layers and train on new data. Feature extraction: freeze all layers, train only the final classifier. Models pre-trained on ImageNet transfer well to medical imaging, satellite photos, etc. In NLP, BERT and GPT are fine-tuned for specific tasks. Reduces training time and data requirements significantly.
